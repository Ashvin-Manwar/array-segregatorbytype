
const {arraySegregator} = require('array-segregator')
 // Example usage:
 const inputArray = [1, 2.5, 'hello', true, false, { a: 1 }, [1, 2, 3], null];
 const segregatedData = arraySegregator(inputArray);
 console.log(segregatedData);
 